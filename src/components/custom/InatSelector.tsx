
type Props = {
    type: 'TAXON' | 'PROJECT';
    select: (id: number) => void;
}

export function InatSelector({ type, select }: Readonly<Props>) {
    // TODO: Implement this
    return (
        <></>
    );
}
